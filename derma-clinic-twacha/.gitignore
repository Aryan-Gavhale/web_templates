node_modules/

# Secrets. Commit .env.example instead.
.env
.env.local

# The database is generated by `npm run seed` and is different on every machine.
data/app.db
data/app.db-shm
data/app.db-wal

# Uploaded photographs and the Google response cache belong to an installation,
# not to the source tree.
data/uploads/
data/cache/

# Process logs
.run/
data/*.log
