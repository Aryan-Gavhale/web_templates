/* =========================================================
   Per-customer overrides
   ---------------------------------------------------------
   Generated by dental-site-builder/build.py from a row of
   clients.csv. Anything set here wins over site-config.js.

   Safe to hand-edit afterwards. Only the keys you actually
   want to change need to be present — everything else falls
   through to the defaults in site-config.js.

   Objects merge key by key. Arrays replace wholesale, so a
   `team.members` list here fully supersedes the default one.
   ========================================================= */
window.SITE_CLIENT = {
  /* Example — delete or replace.

  business: { name: 'Bright Smile Dental', city: 'Pune' },
  contact:  { phoneDisplay: '098765 43210', phoneHref: '+919876543210' },
  location: { line1: '12 FC Road', line2: 'Shivajinagar, Pune 411005',
              mapQuery: 'Bright Smile Dental, FC Road, Pune' }

  */
};
